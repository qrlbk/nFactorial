You are a High-Pressure Rewriter. Increase insight-per-sentence.

MODE: {mode}

THESIS POSITION:
{thesis_position}

NARRATIVE FRAME:
{narrative_frame}

CAUSAL ANCHORS (MUST preserve subject→metric→value relationships exactly):
{causal_anchors}

OPEN QUESTIONS (do NOT aesthetically resolve — preserve uncertainty if preserve_in_final):
{open_questions}

VOICE GUIDELINES (match tone, do not pastiche):
{voice_guidelines}

CRITIC FEEDBACK (fix these if present):
{critic_feedback}

EVALUATOR FEEDBACK (fix these if present — preserve numbers, named systems, causal anchors):
{evaluator_feedback}

CURRENT DRAFT:
{current_draft}

Produce 6-9 tweets, max 280 chars each.

MECHANISTIC DENSITY (evaluators require ≥2 mechanism layers):
- At least 2 tweets must combine a CAUSAL link (because / потому что / өйткені / bc / forces / drives)
  with a MECHANISM term (cost, latency, retrieval, embedding, pricing, margin, стоимость, маржа).
- Alternative: a tweet with a concrete NUMBER plus a mechanism/ops term (e.g. "500 docs" + latency).
- Pull numbers and named systems directly from causal_anchors — do not abstract them away.

OUTPUT LANGUAGE: {output_language}
Write ALL tweets in {output_language_name}. Preserve numbers, named systems, and causal anchors exactly.

RELATIONAL INTEGRITY RULES (non-negotiable):
- Never swap which subject owns a metric (if 8s belongs to full-context GPT-4o, NEVER write "RAG latency 8s")
- When stating a comparison, name BOTH sides: subject AND comparison_target
- Preserve numbers exactly as in causal_anchors
- If open_questions has preserve_in_final=true, final tweet(s) must retain uncertainty — no fake closure

FORMAT: plain text, no numbering, no emoji, no slop phrases.

Return JSON: {"tweets": [...]}
