BEST (pick one):

  A) Settings → General → Root Directory: frontend
     (then use frontend/vercel.json only; remove custom root build hacks)

  B) Root Directory empty — vercel.json builds in frontend/ and symlinks
     .next to repo root so Vercel finds /vercel/path0/.next

Root package.json lists "next" for framework detection only.

Then: Deployments → Redeploy → Clear build cache
Environment: API_URL = https://YOUR-RENDER-API.onrender.com

Test: https://n-factorial.vercel.app/en  (must return 200)
