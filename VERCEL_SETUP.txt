ROOT CAUSE (fixed): .vercelignore had "app/" which excluded frontend/src/app/
→ build showed only Route /404 and .next errors.

=== REQUIRED in Vercel Dashboard ===

1. Framework Preset: Next.js (NOT Python)
2. Root Directory: frontend  ← strongly recommended
3. Environment: API_URL = https://editorial-engine-api.onrender.com
4. Deploy latest main (NOT old commits). Redeploy + Clear build cache.

=== Success log should show ===

  Generating static pages (24/24) or similar — NOT (1/1)
  Routes: /en, /ru, /discover, /voice, ...
  NOT only: Route (pages) ─ ○ /404

=== Test ===

  https://n-factorial.vercel.app/en
  https://n-factorial.vercel.app/api/health
