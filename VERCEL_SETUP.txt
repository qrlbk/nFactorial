VERCEL = Next.js UI ONLY. Python API = Render.

=== OPTION A (BEST) ===

Settings → General → Root Directory: frontend
Framework Preset: Next.js
Environment → API_URL = https://editorial-engine-api.onrender.com

Uses frontend/vercel.json. Do NOT copy .next to repo root.

=== OPTION B (Root Directory empty) ===

Uses root vercel.json with outputDirectory: "frontend"
(build stays in frontend/, Vercel packages frontend/ as the app)

=== NEVER ===

- Framework Preset: Python
- Symlink/copy only .next to repo root (breaks routes → 404 only)

=== Test ===

  https://n-factorial.vercel.app/en          → 200
  https://n-factorial.vercel.app/api/health  → 200 (proxy to Render)
