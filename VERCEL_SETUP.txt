VERCEL DEPLOY — follow exactly or build fails.

=== MANDATORY (Dashboard → Settings → General) ===

  Root Directory:  frontend
  Framework Preset: Next.js
  Node.js Version:   20.x (optional, default OK)

Do NOT leave Root Directory empty. There is no root vercel.json anymore.

=== Environment ===

  API_URL = https://editorial-engine-api.onrender.com

=== Deploy ===

  Git branch: main
  Latest fix: .vercelignore uses /app/ not app/ (so src/app is uploaded)
  Redeploy → Clear build cache

=== Success build log ===

  Detected Next.js version: 16.2.6
  Generating static pages (20+)  — NOT (1/1)
  Routes: /en, /ru, /discover …  — NOT only /404

=== API ===

  Python FastAPI runs on Render only, not Vercel.
