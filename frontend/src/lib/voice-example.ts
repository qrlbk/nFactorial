/** Default writing sample shown in Voice Studio textarea (placeholder + "load example" action). */
export const VOICE_SAMPLE_EXAMPLE = `Everyone thinks inference will get cheaper every year. It won't — GPU bandwidth is the bottleneck, not transistor curves.

Startups that obsess over model choice miss the point. The moat is eval harnesses: if you can't catch regressions before users do, cheaper tokens don't matter.

I don't know whether distillation closes the gap in 24 months. Nobody does. But the unit economics already tell you who captures surplus — and it isn't the buyer of API credits.`;
