{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd frontend && npm ci --include=optional",
  "buildCommand": "cd frontend && npm run build && ln -sfn .next ../.next"
}
